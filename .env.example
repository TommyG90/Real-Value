# Supabase project name: real-value
# Leave blank until the project ref exists. The app runs from data/seed without these.
# Use the secret key only in the load script. Do not prefix it with NEXT_PUBLIC_.

SUPABASE_URL=
SUPABASE_SECRET_KEY=
